server:
  port: 8080

spring:
  datasource:
    hikari:
      connection-timeout: 20000
      maximum-pool-size: 5
    url: jdbc:postgresql://localhost:5432/dev
    username: postgres
    password: postgres
  flyway:
    schemas: users
  jpa:
    properties:
      hibernate:
        default_schema: users
